import AuthForm from "@/src/components/auth/login/auth-form";

export default function Login() {
    return (
        <main className="w-full min-h-screen bg-slate-50 flex justify-center items-center">
            <AuthForm/>
        </main>
    );
}
